"""Analysis package. Every script runs on an artifact tree and nothing else, so
it can be validated on a synthetic tree before real data exists (design I: M6)
and frozen with the harness (D.9). Pure standard library; matplotlib is used
for figures only if installed."""
